const Admin = () => {
  return (
    <div className="bg-gray-100 p-6">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <div className="mt-4">
        <p className="text-blue-500">Total Posts: {10}</p>
        <p className="text-blue-500">Total Users: {10}</p>
        <p className="text-blue-500">Total Views: {10}</p>
        <p className="text-blue-500">Total Comments: {10}</p>
      </div>
    </div>
  );
};

export default Admin;
